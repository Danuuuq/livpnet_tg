user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Лог формата с учетом проброса IP
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for" '
                      'realip="$http_x_real_ip" actualip="$remote_addr"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    # ===== 🔐 Настройка доверенных IP для получения настоящего клиента IP =====
    # Указываем IP хостовой машины (в docker-сети он обычно 172.18.0.1)
    set_real_ip_from 172.18.0.1;
    # Или вся bridge-сеть, если не знаешь точный IP (небезопасно на проде):
    # set_real_ip_from 172.18.0.0/16;

    real_ip_header X-Real-IP;
    real_ip_recursive on;

    server {
        listen 80;
        server_name 127.0.0.1;

        location /subscription/yookassa {
            proxy_pass http://backend:8000/subscription/yookassa;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /webhook/ {
            proxy_pass http://tgbot:9184/webhook;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}